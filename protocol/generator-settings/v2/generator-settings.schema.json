{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/altendky/onshape-export/protocol/generator-settings/v2/generator-settings.schema.json",
  "title": "Onshape Export Generator Settings v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "blockers", "placements"],
  "properties": {
    "schemaVersion": { "const": 2 },
    "blockers": {
      "type": "array",
      "maxItems": 256,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/blocker" }
    },
    "placements": {
      "type": "array",
      "maxItems": 256,
      "items": { "$ref": "#/$defs/placement" }
    }
  },
  "$defs": {
    "objectIdentity": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "pattern": "^[!-~]+$"
    },
    "blocker": {
      "type": "object",
      "additionalProperties": false,
      "required": ["objectIdentity", "targets"],
      "properties": {
        "objectIdentity": { "$ref": "#/$defs/objectIdentity" },
        "targets": {
          "type": "array",
          "minItems": 1,
          "maxItems": 64,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/objectIdentity" }
        }
      }
    },
    "placement": {
      "type": "object",
      "additionalProperties": false,
      "required": ["objectIdentity", "matrix"],
      "properties": {
        "objectIdentity": { "$ref": "#/$defs/objectIdentity" },
        "matrix": {
          "type": "array",
          "minItems": 16,
          "maxItems": 16,
          "prefixItems": [
            { "type": "number" },
            { "type": "number" },
            { "type": "number" },
            { "type": "number" },
            { "type": "number" },
            { "type": "number" },
            { "type": "number" },
            { "type": "number" },
            { "type": "number" },
            { "type": "number" },
            { "type": "number" },
            { "type": "number" },
            { "const": 0 },
            { "const": 0 },
            { "const": 0 },
            { "const": 1 }
          ]
        }
      }
    }
  }
}
